name: Run Test_2 Script

on:
  push:
    branches:
      - test_2

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run script for test_2
        run: bash print_test_2.sh
